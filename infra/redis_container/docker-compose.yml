version: "3.7"

volumes:
  data:
    driver: local

services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - 6379:6379
    volumes:
      - data:/data
    restart: always
